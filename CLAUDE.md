# LunaDianaSchool（レッスン手帳）

LINEオープンチャットのなりきり部屋で使う「アイドル養成学校パロ」のWebサイト。GitHub Pages で公開。

## 構成
- フロントは `index.html` 1ファイル（HTML/CSS/JS すべて）。画像・音声・動画はリポジトリ直下。
- バックエンドは Google Apps Script（GAS）+ スプレッドシート。GASのコードはこのリポジトリに無い。
  - `index.html` の `API_URL` がGASのWebアプリURL。JSONP（`callback=`）で通信。
  - 設定は BBS シート2行目の各列に保存（23列目 = レッスン表示設定 `lessonVis`）。
- この開発環境からGASやスプレッドシートには接続できない。

## ユーザーとの約束
- **GASの変更が必要なときは、変更後のGAS全コードをチャットにそのまま貼る**（ファイル添付ではなくコピペできる形で）。
- 画面上の表示に**絵文字は使わない**。アイコンは Google の Material Symbols Rounded（`mi('name')` ヘルパー / `<span class="mi">name</span>`）を使う。
- 演出は**自然で可愛く**（ふわっと・やわらかい動き。派手すぎない）。
- フォント：本文は Zen Maru Gothic、タイトル類は Mochiy Pop One。
- デザインは「アイカツ！」公式サイト（DCDアンコール）の雰囲気：ピンクのダイヤ柄背景、マゼンタ（`--ak-mag`）の縁取り、白カード、ぷっくりピルのボタン、フリルの区切り、ツヤのあるハート・星・リボン・キラキラ（CSS内のSVG）。見出しは `.ak-heading`（小ラベル＋マゼンタのピル）。公式のロゴや画像は使わない。
- 動きはなめらかに（画面切り替えのフェード、弾むような押し込み）。

## 注意
- 組名（`花の歌組🌸` など）や判定名（`👑 大成功` など）は保存データと照合に使うので、値は変更しない。判定名は表示時に `noEmoji()` で絵文字を除く。
- トランプのマーク（♡♢♤♧）はタイプの記号として残している。
- 組の絵文字（🌸🕊🍃🌙）は例外として絵文字のまま表示する。
